%h1 Projects

.table-responsive
  %table.table.table-hover.table-striped
    %thead
      %th Project ID
      %th Name
    %tbody
      - @projects.each do |project|
        = render partial: project
- if @projects.empty?
  .alert.alert-info
    = t('.no_records')
